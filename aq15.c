#include <stdio.h>
void main(){
     // Q15: Fahrenheit to Celsius
    printf("Enter Fahrenheit: ");
    scanf("%f", &fahrenheit);
    celsius = (5.0 / 9.0) * (fahrenheit - 32);
    printf("Celsius = %.2f\n", celsius);
}