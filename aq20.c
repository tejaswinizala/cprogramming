#include <stdio.h>
void main(){
     float base, height;
    printf("Enter base and height of triangle: ");
    scanf("%f%f", &base, &height);
    area = (base * height) / 2;
    printf("Area = %.2f\n", area);
}